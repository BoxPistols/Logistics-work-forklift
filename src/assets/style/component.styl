/*
  variable setting
*/

//colors
gray-base = #8c97a1
active = teal

// mixin
bd(color)
  border: 2px solid color
// extend
active-form
  bd(active)
  outline none
  background-color lighten(forestgreen, 95%)
